# week-7--d5
The key is never in the code
The key is not on GitHub
The app is safe and works with just the button click

Open the app in a browser
Click “Get Quote”
See the quote appear

Gets a secret key from a proxy
Uses the key to fetch a quote
Shows loading while fetching
Shows error if something goes wrong
